import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';

@Injectable()
export class QuestService {
  constructor() { }

  // getByUserIdMultiple$(userId: string): Observable<GQL.IQuest[]> {
    
  // }
}
